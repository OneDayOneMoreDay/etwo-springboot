#服务器配置
server:
  port: 8081
  servlet:
    context-path: /
  ssl:  #关于https的配置
    key-store: classpath:javaboy.p12
    key-alias: tomcathttps
    enabled: true
    key-store-password: 123456

  tomcat:
    uri-encoding: utf-8

#日志配置
logging:
  level:
    com.jxnu.mapper: debug
    root: info
  file: /mylog/etwo-springboot.log

spring:
  datasource:
    #数据源基本配置
    username: root3
    password: Eeat6666
    url: jdbc:mysql://rm-uf6gwi70o6y17nbiuro.mysql.rds.aliyuncs.com:3306/etwo?characterEncoding=UTF-8&useSSL=false
    driver-class-name: com.mysql.jdbc.Driver
    type: com.alibaba.druid.pool.DruidDataSource

    #对druid数据源的配置
    initialSize: 5
    minIdle: 5
    maxActive: 20
    maxWait: 60000
    timeBetweenEvictionRunsMillis: 60000
    minEvictableIdleTimeMillis: 300000
    validationQuery: SELECT 1 FROM DUAL
    testWhileIdle: true
    testOnBorrow: false
    testOnReturn: false
    poolPreparedStatements: true
    # 配置监控统计拦截的filters，去掉后监控界面sql无法统计，'wall'用于防火墙
    filters: stat,wall,slf4j
    maxPoolPreparedStatementPerConnectionSize: 20
    useGlobalDataSourceStat: true
    connectionProperties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=500

  servlet:
    multipart:  #设置上传文件的大小
      max-file-size: 10MB
      max-request-size: 10MB

  redis: #配置redis
    host: 47.102.207.240



# 配置mybatis
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.jxnu # 注意：对应实体类的路径

#自定义配置
shopImgPath: /images/shopImg/ #店铺图片在硬盘上的位置。windows系统：该项目所在盘根目录下的/images/shopImg/文件夹内，linux系统：/images/shopImg/文件夹内
dishImgPath: /images/dishImg/ #菜品图片在硬盘上的位置。windows系统：该项目所在盘根目录下的/images/dishImg/文件夹内，linux系统：/images/dishImg/文件夹内


